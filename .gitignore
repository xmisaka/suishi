# ============================================================
#  岁时 · 版本库忽略规则
#
#  原则：**细粒度忽略，不整体排除原生工程。**
#  判据是「换台机器 clone 下来，能不能构建出等价的包」——
#  android/ 里有手工维护的签名配置、权限裁剪、单架构设定与
#  图标原生资源，重新 prebuild 会把它们全部冲掉。
#  所以 android/ 入库，只忽略构建产物与机器相关文件。
# ============================================================

# ------------------------------------------------------------
#  依赖
# ------------------------------------------------------------
node_modules/

# ------------------------------------------------------------
#  Expo / Metro 产物
# ------------------------------------------------------------
.expo/
dist/
web-build/
expo-env.d.ts
.metro-health-check*

# ------------------------------------------------------------
#  原生工程
#  入库：app/src/（含 AndroidManifest.xml 与 res/）、app/build.gradle、
#        gradle.properties、settings.gradle、gradlew(.bat)、
#        gradle/wrapper/（含 gradle-wrapper.jar —— 二进制但必须入库）
#  忽略：构建产物、Gradle 缓存、机器相关路径
# ------------------------------------------------------------
/ios
/android/build/
/android/app/build/
/android/app/release/
/android/app/.cxx/
/android/.gradle/
/android/.kotlin/
/android/build-log/
/android/local.properties

# ------------------------------------------------------------
#  凭据与密钥 —— 严禁入库
#  一旦推上去就永久留在历史里，git rm 删不掉
# ------------------------------------------------------------
*.jks
*.keystore
key.properties
*.p8
*.p12
*.key
*.pem
*.mobileprovision
*签名密钥信息.txt

# ------------------------------------------------------------
#  本地环境变量（.example 模板要入库）
# ------------------------------------------------------------
.env
.env.*
!.env.example

# ------------------------------------------------------------
#  工程内工作区：构建日志、探针脚本、个人备忘
# ------------------------------------------------------------
.workbuddy/

# ------------------------------------------------------------
#  脚本缓存
# ------------------------------------------------------------
__pycache__/
*.pyc

# ------------------------------------------------------------
#  编辑器 / 系统垃圾
# ------------------------------------------------------------
.DS_Store
Thumbs.db
*.orig.*
*.tsbuildinfo
npm-debug.*
yarn-debug.*
yarn-error.*
